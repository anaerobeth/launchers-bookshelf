<%= form_for(@book_signup) do |f| %>
  <% if @book_signup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_signup.errors.count, "error") %> prohibited this book_signup from being saved:</h2>

      <ul>
      <% @book_signup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :launcher_id %><br>
    <%= f.number_field :launcher_id %>
  </div>
  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
