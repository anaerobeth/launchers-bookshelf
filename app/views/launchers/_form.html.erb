<%= form_for(@launcher) do |f| %>
  <% if @launcher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@launcher.errors.count, "error") %> prohibited this launcher from being saved:</h2>

      <ul>
      <% @launcher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
